gem install twitter simple-fourchan dm-core dm-aggregates dm-migrations dm-sqlite-adapter timers json yaml isaac nokogiri

settings.yml:

greetings:
  badChance: 5
  bad:
    - "go away"
  goodChance: 10
  good:
    - "hi"
  goodPeople:
    - "user1"
    - "user2"

config.yml:

config:
  server: irc.freenode.net
  port	: 8001
  realname: botname
  nick	: botnick
  default_channel: "#mychannel"
  default_admin	: "user1"
  nickserv_password: "secret"

commands:
  ping: "pong"
